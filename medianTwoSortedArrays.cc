// 2022/2/26
#include <iostream>
#include <vector>
#include <algorithm>

float median(const std::vector<int> x, const std::vector<int> y) {
    int len_x = x.size();
    int len_y = y.size();
    
    return 0.f;
}
int main() {
    std::vector<int> num1({1, 3});
    std::vector<int> num2({2});
    std::vector<int> num3({1, 3});
    std::vector<int> num4({2, 4});
    median(num1, num2);
    return 0;
}

